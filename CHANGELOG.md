# 변경 이력 (Changelog)

MDViewer 의 사용자 관점 변경 사항을 기록합니다.

형식은 [Keep a Changelog](https://keepachangelog.com) 구조를 따르며, 버전 구간은 `v{버전}` git 태그 기준입니다.
이 문서는 릴리스 도구(ReleaseTool)의 노트 확정 단계로 갱신됩니다 (`docs/release-guide.md` 참조).

## [v1.2.1] - 2026-08-18

### 변경

- **관리자 권한 없이 설치됩니다** — 설치 위치가 사용자 폴더 안 `AppData\Local\Programs\SleighMaster\MDViewer` 로 바뀌었습니다. 설치·업데이트·제거가 관리자 권한 확인(UAC) 없이 진행됩니다. 기존 `Program Files` 설치본에서 업그레이드하면 옛 설치본을 제거하는 동안 권한 확인이 마지막으로 1회 뜹니다. 설정·최근 파일 같은 사용자 데이터와 바탕 화면·시작 메뉴 바로 가기는 그대로 유지됩니다.

### 수정

- 설치 중 위치를 직접 바꾸면 고른 폴더 아래에 `SleighMaster\MDViewer` 폴더가 만들어지지 않고 그 폴더에 바로 설치되던 문제를 고쳤습니다. 이제 고른 위치 아래 `SleighMaster\MDViewer` 폴더에 설치됩니다.

## [v1.2.0] - 2026-08-18

### 추가

- **정보 페이지에서 버튼 한 번으로 업데이트** — 설정의 정보 페이지를 열면 새 버전이 있는지 자동으로 확인해 알려 줍니다. 업데이트를 시작하면 모든 창이 다운로드 진행 화면으로 바뀌고, 다운로드가 끝나면 앱이 스스로 종료한 뒤 설치 화면 없이 새 버전으로 다시 실행됩니다 (Windows 관리자 권한 확인 1회). 취소하거나 실패하면 원래 화면으로 돌아가 기존 버전을 그대로 사용합니다.

### 변경

- **설치 파일을 공개 다운로드 페이지에서 배포합니다** — 누구나 최신 설치 파일을 내려받을 수 있습니다. 정보 페이지의 '다운로드 페이지 열기' 버튼도 이 페이지로 연결됩니다.
- **사용 조건을 사용권 계약에 분명히 적었습니다** — 무료 사용은 그대로입니다 (개인·업무·상업 구분 없이, 기기 수 제한 없음). 설치 파일의 재배포·판매와 프로그램 개작은 금지됩니다.

### 수정

- 라인 번호에서 원본 줄 번호를 표시하면 표에 빈 칸이 끼어 내용이 한 칸씩 밀려 보이던 문제를 고쳤습니다. 표는 원래 열 수대로 표시되고, 줄 번호는 표가 시작하는 줄에만 붙습니다.

## [v1.1.1] - 2026-08-11

### 추가

- **파일 트리 닫기 버튼** — 파일 트리 헤더의 닫기 버튼으로 패널을 바로 닫습니다. 닫은 뒤에는 `Ctrl+B` 나 `보기 > 파일 트리` 메뉴로 다시 열 수 있고, 버튼 모양과 툴팁은 개요 패널의 닫기 버튼과 같은 규칙을 따릅니다.

### 변경

- **열린 문서가 없으면 쓸 수 없는 메뉴가 회색으로 표시됩니다** — 눌러도 실행되지 않습니다. 빈 탭·파일을 찾지 못한 탭·불러오기에 실패한 탭도 문서 없음으로 처리되며, 빈 화면이 그대로 인쇄되던 인쇄 항목도 함께 막습니다.
- **다른 창을 보는 동안 뜬 알림이 사라지지 않고 기다립니다** — 파일 갱신·내보내기 완료 같은 알림이 창이 비활성인 동안 남아 있다가, MDViewer 로 돌아온 뒤 3초가 지나면 사라집니다.

### 수정

- 탐색기에서 문서를 더블클릭해 MDViewer 를 실행하면, 그 문서 탭이 이전 세션 탭 사이에 끼고 활성화되지 않던 문제를 고쳤습니다. 이제 맨 뒤 탭으로 놓이고 바로 활성화되며, 이미 실행 중일 때 추가로 여는 경우도 같습니다.

## [v1.1.0] - 2026-08-04

### 추가

- **문서 라인 번호 표시** — 끄기 / 코드블록 번호 / 원본 줄 번호 / 둘 다 중에서 고릅니다. 번호 모양은 미니멀·에디터풍 2 가지, 글꼴 크기도 조절할 수 있습니다.
- **`.txt` 파일 열기** — 드래그&드롭, 열기 다이얼로그, 파일 트리 어디서든 엽니다. 일반 텍스트로 볼지 마크다운으로 렌더할지 설정에서 고릅니다.
- **글꼴 크기 설정** — 메뉴·탭·사이드바 같은 화면 요소 크기와 문서 본문 크기를 따로 조절합니다. 설정 화면에서 바로 미리 봅니다.
- **버튼 설명 툴팁** — 아이콘 버튼에 마우스를 올리면 이름과 설명이 뜹니다. 현재 테마 색을 따르고, 상태에 따라 문구가 바뀝니다.
- **내보내기 (파일 메뉴)** — PDF, PNG 이미지, HTML 로 저장합니다. 긴 문서의 PNG 는 A4 크기 페이지로 나눠 저장합니다.
- **문서 변환 내보내기** — Word(DOCX)·ODT·RTF·EPUB·LaTeX·MediaWiki·reStructuredText·Textile·OPML 로 저장합니다. Pandoc 이 설치돼 있어야 하며, 없으면 설치 안내가 뜹니다.
- **도움말 > 사용법** — 기능 설명과 단축키 표가 담긴 사용법 문서를 새 탭으로 엽니다.
- **설정 기본값으로 초기화** — 설정 화면 아래 버튼으로 모든 설정을 처음 상태로 되돌립니다. 누르면 확인을 먼저 묻습니다.
- **탭바 표시 방식 선택** — 탭이 많을 때 가로로 스크롤할지 여러 줄로 접을지 고릅니다.

### 변경

- **설정이 별도 창 대신 문서 탭으로 열립니다** — 다른 탭처럼 전환·닫기·`Ctrl+W` 가 됩니다. 적용을 눌러도 탭이 닫히지 않습니다. 타이틀바 옵션 이름은 '컴팩트' 에서 '햄버거' 로 바뀌었습니다.
- **파일·폴더·저장 다이얼로그가 앱 테마와 같은 모습으로 바뀌었습니다** — 다크·라이트를 따라가고, 키보드만으로 쓸 수 있습니다. 폴더 뒤로/앞으로 이동이 생겼고, 왼쪽 목록이 즐겨찾기·드라이브·네트워크·이동식으로 나뉩니다.
- **첫 실행이 빨라졌습니다** — 처음 문서가 뜨기까지 약 2.5 초에서 약 0.6 초로 줄었습니다.
- **기본값 조정** — 테마 기본값이 밝은 테마에서 시스템 설정 따라가기로, 개요 접기/펼치기 효과가 없음에서 서서히로 바뀌었습니다.
- **설치 위치가 `Program Files\SleighMaster\MDViewer` 로 바뀌었습니다** — 업그레이드하면 옛 폴더를 정리하고 설정을 옮겨 옵니다.

### 수정

- 메뉴를 펼친 뒤 하위 메뉴로 마우스를 옮기면 메뉴가 사라지던 문제를 고쳤습니다.
- 마지막 탭을 닫아 프로그램을 끝낸 뒤 다시 실행하면 닫았던 문서가 되살아나던 문제를 고쳤습니다.

## [v1.0.0] - 2026-07-15

MDViewer 첫 정식 출시.
